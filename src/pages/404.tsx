import { Title } from '../styles/pages/Home';

export default function NotFound() {
    return (
        <main>
            <section>
                <Title>Pagina não existe</Title>
            </section>
        </main>
    );
}
